name: test
channels:
  - ambermd
dependencies:
    # Base depends
  - python
  - pip
  - jinja2
  - numpy
  - statsmodels
  - mdtraj
  - pydantic
  - matplotlib
  - pytraj
  - pymbar
  - django
  - oslo.concurrency
  - multiprocess

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  - pip:
    - gnuplotlib
    - numdifftools
    - psutil

