name: test
channels:
dependencies:
    # Base depends
  - python
  - pip
  - jinja2
  - numpy
  - statsmodels
  - mdtraj

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  - pip:
    - pytraj

